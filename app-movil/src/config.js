// URL base del Backend Core (Joshua). En un dispositivo físico o emulador,
// "localhost" apunta al propio dispositivo, no a tu PC — reemplaza por la IP
// de tu red local (ej. "http://192.168.1.10:3000") si no usas el emulador.
export const API_BASE_URL = "http://localhost:3000/api";
export const SOCKET_URL = "http://localhost:3000";